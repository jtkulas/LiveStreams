---
title: "Untitled"
format: html
---
```{r}
library(ggplot2)
```

## Quarto {#sec-main}

See the pretty map in @fig-nightmap

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

:::{#wrn-aliensarecoming .callout-warning}

Oh no lookout AGAIN!!
:::

:::{#wrn-academicstalkweird .callout-warning}

Oh no lookout!!
:::


$$
\alpha
$$ {#eq-alpha}

```{r}
#| label: fig-nightmap
#| fig-cap: This is Fracais at Nite
#| warning: false
#| lst-label: lst-mapcode
#| lst-cap: This is code to NOT zoom in on Spain

# Library
# create a dataset
data <- data.frame(
  name=c( rep("A",500), rep("B",500), rep("B",500), rep("C",20), rep('D', 100)  ),
  value=c( rnorm(500, 10, 5), rnorm(500, 13, 1), rnorm(500, 18, 1), rnorm(20, 25, 4), rnorm(100, 12, 1) )
)

# Most basic violin chart
ggplot(data, aes(x=name, y=value, fill=name)) + # fill=name allow to automatically dedicate a color for each group
  geom_violin()

```


hey here's my document!!! Look at @wrn-academicstalkweird

a fancy symbol is located in @eq-alpha

if you want to see some empty boilerplate info please go to [-@sec-main]

See the pretty map in @fig-nightmap

the code is hidden in [Code @lst-mapcode]
